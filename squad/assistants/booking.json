{
  "name": "Booking",
  "tools": [
    {
      "type": "function",
      "function": {
        "name": "get_case_status",
        "parameters": {
          "type": "object",
          "required": ["last_user_message"],
          "properties": {
            "last_user_message": {
              "type": "string",
              "description": "The user's most recent message, exactly as they said it"
            }
          }
        },
        "description": "Get case status and determine how to respond. ALWAYS pass the user's last message."
      }
    },
    {
      "type": "function",
      "function": {
        "name": "get_session_summary",
        "parameters": { "type": "object", "required": [], "properties": {} },
        "description": "Get the accumulated service list for this call."
      }
    },
    {
      "type": "function",
      "function": {
        "name": "store_service_order",
        "parameters": { "type": "object", "required": [], "properties": {} },
        "description": "Store all accumulated services as service orders."
      }
    },
    {
      "type": "function",
      "function": {
        "name": "send_confirmation_sms",
        "parameters": {
          "type": "object",
          "required": ["phone_number", "message"],
          "properties": {
            "message": { "type": "string", "description": "SMS body" },
            "phone_number": { "type": "string", "description": "Phone number to text" }
          }
        },
        "description": "Send an SMS confirmation after booking is completed."
      }
    }
  ]
}
